{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Active File",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}.exe",
            "cwd": "${fileDirname}",
            "console": "integratedTerminal",
            "stopAtEntry": false,
            "MIMode": "gdb"
        }
    ]
}
